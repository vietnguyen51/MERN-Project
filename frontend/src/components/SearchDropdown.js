const SearchDropdown = ({ onClose }) => {
  return (
    <div>
      {/* Nội dung của SearchDropdown */}
      <button onClick={onClose}>Close Search</button>
    </div>
  );
}

export default SearchDropdown;
